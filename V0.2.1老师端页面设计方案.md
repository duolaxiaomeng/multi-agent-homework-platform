# 老师端核心页面设计方案：课次首页 + 课次工作台

> 版本：V0.2.1 设计稿（2026-07-25）
> 范围：仅老师端两个核心页面——课次首页（home）与课次工作台（workspace），含必要的配套小改动。
> 设计依据：`V0.2开发规范-以课次为核心.md`、现状代码（index.html / app.js / server.py）。

---

## 1. 设计目标

围绕一节课，老师从打开系统到收工的路径只有一条：

```
打开系统 → 一眼看到今天/未完成的课 → 点进工作台 →
按页签走完 ①考勤 ②收作业 ③确认 ④看学情 ⑤发反馈 → 收工
```

衡量标准（对应规范最终体验标准）：

1. 老师不需要理解多个分散页面之间的关系；
2. 任何时刻都能回答三个问题：**现在进行到哪一步、下一步该做什么、有没有必须人工处理的事**；
3. 每节课的高频操作（考勤、上传、确认）点击次数最少。

非目标（本期不做）：学生端改版、Agent 会话页改版、数据备份导出、移动端适配、新模型接入。

---

## 2. 现状主要问题（本方案要解决的）

| # | 问题 | 影响 |
|---|---|---|
| P1 | 开课有两处入口（首页开课 / 导入作业第 1 步），且首页开课无重复课次防呆 | 重复建课，作业考勤散落两个课次，难以收拾 |
| P2 | 课次状态基本靠手动切换；`recompute_lesson_status` 只在 analyzing/pending/completed 段自动 | 忘切状态 → 首页卡片显示错误进度，老师失去对"进行到哪一步"的信任 |
| P3 | 首页卡片全部一个"进入工作台 →"，无状态差异；无今日/历史分组；无删除 | 课次一多就找不到今天的课；也无法判断哪节课有急事（待确认） |
| P4 | 考勤用每生一个下拉框，改 1 人点 2 下 | 40 人班级逐个点很慢，这是每节课必做的最高频操作 |
| P5 | 作业上传：每行一个 file 框拥挤；上传状态不可见（上传中会误以为卡死）；无粘贴 | 收作业是第二高频操作，体验粗糙且易重复提交 |
| P6 | 待确认审核看不到作业原图 | 老师无法核实 AI 判断，"确认"变成盲签 |
| P7 | 术语混用："课程 / 分析任务 / 课次"并存（如"请选择分析任务"） | 新用户困惑，违背规范 8.1 的统一语义要求 |
| P8 | 反馈页编辑后没有"另存"，复制按钮只在生成文件时出现 | 编辑完恰恰最需要复制/保存，逻辑反了 |

---

## 3. 信息架构：页面收敛

老师端主导航由 9 项收敛为 4 项 + 2 个页内入口：

```
课次（首页，默认落地页）
总览
班级与学生
设置（含 Agent 灵魂设定、账号管理*）
```

旧页面处理方式（对应规范 5.3"旧页面不得成为新流程的主要入口"）：

| 旧导航项 | 去向 |
|---|---|
| 课堂座位 | 并入工作台①课堂记录（直接内嵌座位图，不再跳转独立页面） |
| 导入作业 | 并入工作台②课后作业；课堂内容报告上传移到工作台头部（每节课一次） |
| 人工补充 | 并入工作台③待确认页底部"手动补充一条"折叠区 |
| 总结 Agent | 收敛为工作台⑤学生反馈；Agent 会话保留为设置页的"高级"入口（规范第 13 条：智能体配置放高级设置） |
| 账号管理 | 管理员在设置页内看到（现在已是隐藏导航，搬入即可） |

术语统一（全站替换，含占位符、空状态文案、错误提示）：

- "分析任务" → **课次**
- "课程"（指 assignment 的场合）→ **课堂内容**（指一节课的主题与知识点）；下拉选项格式统一为 `班级 · 日期 · 第N节 · 课名`
- 数据层 `assignments` 字段本期不改名（避免数据迁移风险），只改 UI 文案。

---

## 4. 课次首页设计

### 4.1 页面结构（自上而下）

```
┌─────────────────────────────────────────────────┐
│ 开课条：班级 ▾ | 日期(默认今天) | 第N节(智能建议) │
│         | 课名(可空) | [＋开课]                  │
├─────────────────────────────────────────────────┤
│ 分组一：今天的课（按节次排序）                    │
│ 分组二：待处理（非今天、状态≠已完成的课）          │
│ 分组三：已完成的课（折叠，默认收起，显示数量）      │
├─────────────────────────────────────────────────┤
│ 课次卡片 ×N                                      │
└─────────────────────────────────────────────────┘
```

### 4.2 智能节次建议

开课时系统根据该班今天已有的课自动建议下一节次：取该班当天最大节次 +1（跨天默认第 1 节）。老师可改。班级切换时实时刷新建议值。

### 4.3 重复课次防呆（P1）

提交前检查同班 + 同日期 + 同节次是否已有课次：有则弹确认"该时段已有《××》课，仍要再建一节吗？"（复用导入页已有的 clash 逻辑）。双击防护：按钮提交即 disabled。

### 4.4 课次卡片（P3）

每张卡片：

```
┌───────────────────────────────────┐
│ 初二(3)班            [状态徽章]    │
│ 07-25 第2节 《分数的加减法》        │
│ 应到 40｜2人请假                   │
│ 作业 32/40｜已分析 30              │
│ ⚠ 待确认 4 项        （红色强调）   │
│ [下一步动作按钮]        [⋯ 删除]   │
└───────────────────────────────────┘
```

**下一步动作按钮按状态渲染**（规范 5.1 要求的"下一步操作"）：

| 课次状态 | 按钮文案 | 点击后 |
|---|---|---|
| 上课中 | 继续上课 → | 工作台①课堂记录 |
| 等待作业 | 去收作业 → | 工作台②课后作业 |
| 作业分析中 | 查看分析进度 → | 工作台②课后作业 |
| 待确认 | 去确认 N 项 →（主色+红色角标） | 工作台③待确认 |
| 已完成 | 查看学情与反馈 → | 工作台④班级学情 |

**待确认是全站唯一必须人工处理的事**，在卡片上用红色文字 + 按钮角标双重强调；有待确认的卡片排在各分组最前。

**删除课次**：卡片 ⋯ 菜单内，二次确认（规范 16.4），说明"将同时删除这节课的考勤、作业记录与错题"。

### 4.5 空状态

- 无任何课次：插画式空态 + "先在上方开课"（现状已有，保留）。
- 今天没课但有待处理课：今天分组显示"今天没有课，下面还有 N 节课待处理"。

---

## 5. 课次工作台设计

### 5.1 头部（常显，规范 5.2 要求）

```
┌──────────────────────────────────────────────────────┐
│ ← 课次列表   初二(3)班 · 07-25 第2节                  │
│              课名《分数的加减法》 ✎                   │
│ [状态徽章▾自动]  作业 32/40   待确认 4               │
│ [① 课堂记录] [② 课后作业] [③ 待确认·4] [④ 班级学情] [⑤ 学生反馈] │
└──────────────────────────────────────────────────────┘
```

- **课名可就地编辑**（铅笔图标，失焦保存）——现在课名建完就不能改。
- **页签 ③ 上直接显示待确认数量徽章**，老师在任何页签都能看到还有几件事要处理。
- 课堂内容报告：头部"课堂内容"区域显示本节课主题与知识点；未上传时显示"上传课堂报告（可选）"入口，每节课一次，上传后 AI 自动读主题（复用现有 `/api/lessons` 报告逻辑，见 7.2 接口新增）。

### 5.2 状态自动流转（P2，核心改动）

原则：**系统自动推进为主，手动纠正为辅**。老师在大多数情况下不需要碰状态下拉框。

| 时机 | 自动动作 |
|---|---|
| 开课 | `in_class`（现状） |
| 首次保存考勤/课堂记录后，老师在①点"下课，开始收作业" | `waiting_homework`（显式按钮，替代手动切换） |
| 任一作业发起分析 | `analyzing`（现状已有） |
| 分析结束有待确认 | `pending_review`（现状已有） |
| 待确认清零且全部已分析 | `completed`（现状已有） |
| 已完成课次收到补交 | 回退 `waiting_homework`（现状已有） |

UI 上状态下拉框降级为"状态徽章 + 点击展开可手动改"，旁边一行小字说明"状态会自动更新"。手动修改仍然保留（规范允许特殊情况），但不再是必经路径。

①课堂记录页顶部放置流程主按钮，随状态变化：

- `in_class` → 显示「下课，开始收作业 →」（点击切状态并跳到②）
- 其他状态 → 显示当前进度提示条："这节课正在收作业（32/40）"

### 5.3 页签① 课堂记录

上半部分考勤，下半部分内嵌座位图（不再需要"打开座位图记录 →"跳转）。

**考勤区改版（P4）**：

```
应到 40｜已到 38｜迟到 1｜请假 1        [全部已到]
─────────────────────────────────────
张小明  [已到|迟到|请假|缺勤|早退]       ←  segmented 按钮组
李小红  [已到|迟到|请假|缺勤|早退]
```

- 每个学生一排五段式小按钮，点 1 下即改，立即保存（接口不变，逐条 POST）。
- 默认全"已到"，只有特殊情况的老师才需要点；当前状态高亮。
- 「全部已到」一键重置（二次确认），用于误操作后恢复。
- 缺勤/请假学生的行加灰底，与作业页②联动置灰（提示"该生缺勤，无需收作业"，但不禁止补录——规范允许事后补交）。

**座位图区**：复用现有座位组件，两个改动：

1. 课程选择器移除——进入工作台即默认关联本节课（现在还要老师再选一次课程，容易选错）；
2. 座位单元上叠加考勤状态小角标（迟/假/缺），让座位图同时成为考勤的第二种操作面（规范第 6 条要求"支持座位图操作考勤"）。

### 5.4 页签② 课后作业

```
[✦ 分析全部待分析作业]   已收 32/40｜已分析 30   ☑连续录入
──────────────────────────────────────────────
张小明  [已上传·3张]  [粘贴/选图追加] [AI分析] ── 正常行
李小红  [未收取]      [粘贴 Ctrl+V / 选择图片]  ── 待收行高亮
王缺勤  [缺勤·无需收]  [仍可补交]               ── 灰底
```

改进点：

1. **支持 Ctrl+V 粘贴截图**（P5）：每行上传区与座位图一致支持粘贴；老师从微信收到的作业照片截图即传。
2. **上传中状态**：点击上传后该行显示"上传中 1/3 张…"，行级禁用，防止重复提交（P5）。
3. **行排序**：未收取的学生排在前面，已完成的沉底；老师从第一名学生往下传即可，配合连续录入自动跳下一位。
4. **分析失败可见**：整课次分析后，失败的学生行内显示"分析失败：原因 [重试]"，不只是一句 toast（规范 16.5：不得静默失败；16.6：单人失败不影响整课——后端已满足，前端补展示）。
5. 作业照片可点开缩略图查看（现在传完就看不见了，无法核对传的是谁的）。

### 5.5 页签③ 待确认（P6 证据对照）

每条待确认项改为"左图右文"的可展开卡片：

```
┌──────────────────────────────────────────────┐
│ ☐ 张小明                    置信度 0.62       │
│ ┌─────────┐  题目: [第3题：3/4+1/5    ]      │
│ │ 作业原图 │  知识点: [分数加减法      ]      │
│ │ (可放大) │  类型: [计算错误 ▾]             │
│ └─────────┘  AI依据: 第二步通分错误…          │
│ [✓确认] [保存修改] [忽略] [不是错题]          │
└──────────────────────────────────────────────┘
```

- 错题数据已有 `image` 字段（`analyze_submission` 写入 `submission["images"][0]`），直接渲染即可，无需新接口。多图作业显示第一张 + "共 N 张"可切换。
- 顶部保留"合并选中题目"；新增「全部确认」按钮（二次确认）用于 AI 质量高时快速清场。
- 底部折叠区"手动补充一条错题"（迁移原人工补充页表单，课次已固定为本节，学生下拉只列本班）。

### 5.6 页签④ 班级学情

结构基本保留现状（考勤卡、知识点错误人数、高频错题、错误类型、重点关注学生、不计入统计分组），补两个体验点：

1. **证据下钻**（规范第 11 条"每项统计应能够下钻"）：知识点条形、高频错题行可点击，弹出该知识点/题目对应的学生名单与错题明细（数据在 `/api/lessons/:id` 的 mistakes 里已有，前端聚合即可）。
2. 待确认 > 0 时页顶提示"还有 N 项待确认，当前统计不含未确认结果 → 去确认"。

### 5.7 页签⑤ 学生反馈（P8）

保留"选学生 → 生成 → 预览/编辑"流程，修正保存逻辑：

```
[生成反馈] → 预览（渲染版） + [复制全文] [下载MD]
             编辑区（改动后）→ [保存到我的修改] [重新生成]
```

- **复制按钮常显**，不再依赖是否生成了文件。
- 新增"保存修改"：把老师编辑后的版本以 `反馈-姓名-日期-修订.md` 另存（不覆盖 AI 原稿，符合规范 12"不得覆盖原始证据"）。
- 特殊情形（缺勤/未交/分析中）后端返回的客观模板文案保持现状——这是规范红线，UI 上把这些反馈与 AI 生成的用不同底色区分，并标注"此反馈由系统根据考勤与作业状态自动生成"。

---

## 6. 老师的一天：改前 vs 改后

| 环节 | 改前 | 改后 |
|---|---|---|
| 早上开课 | 填班级/日期/节次，手快可能建重 | 节次自动建议，重复有提醒 |
| 上课考勤 | 40 个下拉框逐个点 | 默认全到，只点 2 个特殊学生 |
| 课堂记录 | 跳去座位页、还要再选一次课程 | 工作台内嵌座位图，自动关联本节课 |
| 下课 | 想起要切状态（常忘） | 点"下课，开始收作业"一个按钮 |
| 收作业 | 每行 file 框逐个点开找文件 | 截图 Ctrl+V 直贴，未交的排在最前 |
| 分析 | 手动切"分析中"状态 | 点分析即自动切 |
| 确认 | 对着文字盲签 | 左图右文对照确认 |
| 发反馈 | 编辑完只能复制（按钮还时有时无） | 复制常显，可另存修订版 |

---

## 7. 技术方案

### 7.1 前端（index.html / app.js / style.css）

1. **导航收敛**：隐藏旧导航项（DOM 保留、代码暂存一版），home/workspace 成为主流程；`#quick-add` 按钮改为跳转工作台③待确认（或移除）。
2. **workspace 页签渲染器重构**：`renderWsRecord` 内嵌座位图组件（把现有 renderSeats 提炼为可复用函数，去掉页面级依赖）；新增考勤 segmented 控件与"全部已到"。
3. **首页分组渲染**：`loadHome` 返回的 cards 按 `date === today`、`status !== 'completed'` 分三组渲染；卡片增加 `nextAction` 计算（纯前端映射，见 4.4）。
4. **待确认卡片**：`renderWsReview` 每条渲染 `m.image`（`/uploads/...` 静态路径，已有鉴权豁免——注意确认：当前 `/api/**` 之外静态文件直接由 SimpleHTTPRequestHandler 提供，图片可直接 `<img src>`）。
5. **粘贴上传**：复用 `uploadSeatImage` 的 FileReader→`/api/upload` 逻辑，给作业行加 paste 监听。

### 7.2 后端（server.py，小改）

| 改动 | 说明 |
|---|---|
| `POST /api/lessons` 增加冲突检查 | 同班+日期+节次已存在时返回 `409 {conflict: 已有课次信息}`，前端弹确认后带 `force: true` 重发 |
| `GET /api/lessons` 卡片增加 `nextAction` | 由状态直接映射，前端省一份逻辑（也可纯前端做，二选一） |
| `POST /api/lessons/:id/report`（新增） | 为已存在的课次补传课堂报告（现在只能在创建时传），复用 `REPORT_PROMPT`，回写 assignment 的 title/content/knowledgePoints |
| `PATCH /api/lessons/:id`（新增） | 修改课名/日期/节次（课名就地编辑用） |
| `DELETE /api/lessons/:id`（新增） | 删除课次及其 attendance/submissions/mistakes（关联 performances 保留但解除关联），二次确认在前端 |
| `POST /api/mistakes/review-all`（新增，可选） | 批量确认，避免"全部确认"逐条调接口 |
| `ensure_lesson_attendance` | 现状是在课次创建时生成；改为在"下课，开始收作业"前任意时刻允许补考勤即可，无需改动 |

状态流转逻辑（`recompute_lesson_status`）已覆盖 analyzing 之后的自动推进，**后端无需大改**——主要补"下课"这个显式动作的前端按钮。

### 7.3 数据兼容

- 不动 `data.json` 结构，不新增迁移；旧课次数据照常显示。
- 导航收敛后旧数据入口（总览页、人工补充的数据）全部仍可通过新页面触达。

### 7.4 开发顺序（对应规范第 17 条的阶段思想）

| 阶段 | 内容 | 验收 |
|---|---|---|
| 第一步 | 首页三分组 + 卡片下一步按钮 + 重复防呆 + 智能节次 | 建 3 节课验证分组、建议节次、冲突提示 |
| 第二步 | 工作台①：考勤 segmented + 全部已到 + 内嵌座位图 | 一节课内完成考勤+座位记录不跳页 |
| 第三步 | 工作台②：粘贴上传、行排序、上传中/失败行内状态 | 混合上传 5 人（含 1 次故意失败）验证 |
| 第四步 | 工作台③：带图确认卡 + 全部确认 + 手动补充折叠区 | 确认后首页徽章同步清零、状态自动 completed |
| 第五步 | 工作台⑤：复制常显 + 另存修订 + 导航收敛 + 术语统一 | 全流程走一遍，旧导航项全部移除 |
| 第六步 | 学情下钻 + 反馈模板底色区分 | 点知识点能弹出学生明细 |

每步完成后用真实本地数据验证（规范 16.3），前端改动刷新即可，改 server.py 需重启。

### 7.5 不做清单（本方案明确排除，对应规范第 14 条）

- 学生端改版、家长端、云同步、排课系统
- AI 识别姓名/匹配名单/生成课程名称
- 考勤与作业状态的互相推断
- 数据备份导出（列入下一迭代候选）

---

## 8. 风险与注意

1. **内嵌座位图的复杂度**：座位组件目前依赖页面级全局变量（`seatClass`、`selSeat`），内嵌到工作台时需要参数化，建议单独一步做，避免和考勤改动搅在一起。
2. **导航收敛的回撤成本**：保留旧页面代码一个版本（注释或隐藏），老师试用新流程一周无异议后再删。
3. **批量确认按钮**与"老师不需要逐条审核高置信度结果"的规范一致，但要防误触，必须二次确认且仅在待确认 > 3 时出现。
4. 规范 15.6 要求"修改前应自动生成本地备份"——本次不动数据结构，但删除课次接口上线前，建议在 server.py 写一个通用的 `backup_data()`（每次启动时若 data.json 距上次备份超过 24 小时则复制一份），为后续迭代的删除类操作兜底。
