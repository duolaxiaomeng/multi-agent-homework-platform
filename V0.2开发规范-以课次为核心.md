# 学生管理与统计系统 V0.2 开发规范

## 1. 版本主题

本版本以“一节课（课次）”为核心重组现有功能。

产品目标：

> 老师创建一节课，记录考勤和必要的课堂表现；学生下课后完成并上交作业，老师按学生上传作业；AI 分析作业；老师只确认不确定内容；系统最终生成班级学情和学生反馈。

本版本重点是打通完整教学闭环，不继续横向增加无关功能。

---

## 2. 核心原则

1. 所有考勤、课堂表现、作业、错题和报告都必须归属于某一节课。
2. 学生作业在本节课结束、学生完成并上交后，由老师上传。
3. 学生与作业的对应关系由老师明确指定。
4. 不使用 AI 识别学生姓名。
5. 不使用 AI 将作业自动匹配到学生名单。
6. 不需要由 AI 生成或补充课程名称。
7. AI 只分析老师已指定学生的作业内容。
8. 课堂记录为可选操作，不能妨碍老师正常授课。
9. AI 无法确定时必须进入“待确认”，禁止编造结论。
10. 所有学情结论应尽量能够追溯到考勤、课堂记录或作业图片。

---

## 3. 标准业务流程

### 阶段一：创建课次

老师选择班级，系统自动填写当前日期和建议节次，老师可以修改。

创建课次后，系统自动加载：

- 班级学生名单；
- 班级座位布局；
- 默认考勤记录；
- 当前课次工作台。

课次初始状态为 `in_class`（上课中）。

### 阶段二：记录考勤

系统默认所有学生为“已到”，老师只修改特殊情况。

考勤状态：

- `present`：已到；
- `late`：迟到；
- `leave`：请假；
- `absent`：缺勤；
- `early_leave`：早退。

考勤可以通过学生名单或座位图修改。每名学生在每个课次中只能有一条考勤记录。

### 阶段三：记录课堂表现

上课过程中，老师可通过座位图记录需要关注的学生。

允许记录：

- 表现标签；
- 课堂备注；
- 做题情况备注；
- 课堂做题过程图片。

课堂表现不是必填项。没有课堂记录不得自动解释为表现正常或表现异常。

### 阶段四：等待并收取作业

下课后，老师将课次切换为 `waiting_homework`（等待作业）。

学生完成并上交作业后，老师从班级名单中选择学生，再为该学生上传一张或多张作业图片。

必须支持：

- 同一学生一次上传多张图片；
- 同一学生分批追加图片；
- 补交作业；
- 一批学生分多次处理；
- 明确显示每名学生的作业状态。

不得根据图片中的姓名自动改变老师选定的学生。

建议提供两种操作方式：

1. 单个上传：选中学生后上传其作业；
2. 连续录入：保存当前学生后自动进入名单中的下一名学生。

### 阶段五：AI 分析

老师对指定学生的作业发起分析。课次进入 `analyzing`（分析中）。

AI 负责识别：

- 题目或题号；
- 作答结果；
- 是否存在错误；
- 对应知识点；
- 错误类型；
- 判断依据；
- 置信度。

AI 不负责：

- 识别学生姓名；
- 匹配班级名单；
- 修改作业所属学生；
- 生成课程名称；
- 在没有图片证据时推断学生作业情况。

下列结果必须转为待确认：

- 置信度低于系统阈值；
- AI 无法判断答案正误；
- 题目内容无法识别；
- 知识点或错误类型不确定；
- AI 明确标记为不确定；
- 疑似重复结果；
- 学生存在不同但可能正确的解法。

### 阶段六：老师确认

课次存在未确认结果时，状态为 `pending_review`（待确认）。

老师应能对待确认项执行：

- 确认；
- 修改；
- 忽略；
- 标记“不是错题”；
- 修改题目描述；
- 修改知识点；
- 修改错误类型；
- 合并重复题目。

老师不需要逐条审核高置信度且无冲突的结果。

### 阶段七：形成学情

确认完成后，课次可以进入 `completed`（已完成）。

系统生成：

- 班级学情；
- 学生个体反馈。

报告可以重新生成，但原始考勤、课堂记录、作业图片和老师确认结果不得被报告生成操作修改。

---

## 4. 课次状态

统一使用以下状态：

| 状态值 | 中文名称 | 含义 |
|---|---|---|
| `in_class` | 上课中 | 正在进行考勤或课堂记录 |
| `waiting_homework` | 等待作业 | 已下课，等待学生完成并上交作业 |
| `analyzing` | 作业分析中 | AI 正在处理已上传作业 |
| `pending_review` | 待确认 | 存在需要老师处理的分析结果 |
| `completed` | 已完成 | 当前课次的主要处理流程已完成 |

状态流转原则：

```text
上课中 → 等待作业 → 作业分析中 → 待确认 → 已完成
```

允许以下特殊情况：

- 没有待确认项时，分析完成后可以直接进入“已完成”；
- 已完成课次允许补交作业；
- 补交作业后，课次重新进入“作业分析中”或“待确认”；
- 老师可以手动结束课堂并进入“等待作业”；
- 状态变化不得删除已有数据。

---

## 5. 页面与导航规范

### 5.1 首页

首页以“课次任务”作为主要内容，不再优先展示彼此分散的功能入口。

每张课次卡片至少显示：

- 班级；
- 日期和节次；
- 当前状态；
- 考勤摘要；
- 作业收交进度；
- 分析进度；
- 待确认数量；
- 下一步操作按钮。

示例：

- 三年级一班｜今天第 2 节｜2 人请假，6 人未交作业；
- 五年级二班｜分析完成｜还有 4 项待确认；
- 四年级一班｜已完成｜查看班级学情与学生反馈。

### 5.2 课次工作台

座位、考勤、课堂表现、作业上传、错题确认、统计和反馈统一从课次工作台进入。

工作台建议使用以下页签或步骤：

1. 课堂记录；
2. 课后作业；
3. 待确认；
4. 班级学情；
5. 学生反馈。

老师进入工作台后，应始终能够看到：

- 当前班级；
- 日期和节次；
- 当前课次状态；
- 作业完成进度；
- 待确认数量。

### 5.3 旧页面处理

现有功能不得简单删除，应逐步迁移到课次工作台：

- 座位图迁入“课堂记录”；
- 作业导入迁入“课后作业”；
- 错题列表迁入“待确认”及学生详情；
- 统计迁入“班级学情”；
- Agent 生成能力迁入“班级学情”和“学生反馈”。

旧页面可以暂时保留兼容入口，但不得继续成为新流程的主要入口。

---

## 6. 考勤规范

每个课次创建时，为该班当前学生生成默认“已到”状态，或在读取时按“已到”展示并在首次修改时落库。

考勤功能要求：

- 支持名单视图；
- 支持座位图操作；
- 支持快速切换状态；
- 显示应到、实到、迟到、请假、缺勤和早退人数；
- 保存修改时间；
- 记录操作老师；
- 新加入班级的学生不应自动污染历史课次名单；
- 删除学生时不得直接删除历史考勤证据。

班级学情和学生反馈必须区分：

- 缺勤导致无课堂证据；
- 到课但未提交作业；
- 已提交但尚未分析；
- 已分析且有明确错误。

---

## 7. 作业收交规范

考勤状态与作业状态必须分开存储。

建议的学生作业状态：

| 状态值 | 中文名称 |
|---|---|
| `not_submitted` | 未收取 |
| `submitted` | 已收取 |
| `uploaded` | 已上传 |
| `analyzing` | 分析中 |
| `pending_review` | 待确认 |
| `completed` | 已完成 |
| `not_required` | 无需提交 |

每份学生作业必须明确关联：

- 课次 ID；
- 学生 ID；
- 上传图片；
- 当前状态；
- 上传时间；
- 分析时间；
- 分析结果。

系统不得用考勤状态推断作业状态，也不得用作业状态推断考勤状态。

---

## 8. 建议数据结构

可以在保持现有数据兼容的前提下，新增或调整以下结构。

### 8.1 课次 `lessons`

```json
{
  "id": "lesson_xxx",
  "classId": "class_xxx",
  "date": "2026-07-24",
  "period": "2",
  "status": "waiting_homework",
  "createdBy": "user_xxx",
  "createdAt": "ISO 时间",
  "updatedAt": "ISO 时间"
}
```

如果继续复用现有 `assignments` 作为课次数据，必须统一其语义，并逐步迁移为 `lessons`，避免“课程、作业、分析任务”混用同一个概念。

### 8.2 考勤 `attendance`

```json
{
  "id": "attendance_xxx",
  "lessonId": "lesson_xxx",
  "studentId": "student_xxx",
  "status": "present",
  "note": "",
  "updatedBy": "user_xxx",
  "updatedAt": "ISO 时间"
}
```

唯一约束：`lessonId + studentId`。

### 8.3 学生作业 `submissions`

```json
{
  "id": "submission_xxx",
  "lessonId": "lesson_xxx",
  "studentId": "student_xxx",
  "status": "uploaded",
  "images": ["uploads/work_xxx.png"],
  "createdAt": "ISO 时间",
  "updatedAt": "ISO 时间"
}
```

唯一约束建议为：`lessonId + studentId`。同一学生后续上传的图片追加到同一提交记录。

### 8.4 错题 `mistakes`

现有错题结构保留，但必须明确增加或保证：

```json
{
  "lessonId": "lesson_xxx",
  "submissionId": "submission_xxx",
  "studentId": "student_xxx",
  "question": "第 3 题",
  "knowledgePoint": "知识点",
  "errorType": "计算错误",
  "status": "pending",
  "confidence": 0.72,
  "reason": "AI 判断依据",
  "image": "uploads/work_xxx.png",
  "reviewedBy": "",
  "reviewedAt": ""
}
```

---

## 9. 接口规范

具体路径可以根据现有代码调整，但能力必须完整。

建议新增或重构：

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/api/lessons` | 创建课次 |
| `GET` | `/api/lessons/:id` | 获取课次工作台全部数据 |
| `POST` | `/api/lessons/:id/status` | 修改课次状态 |
| `GET` | `/api/lessons/:id/attendance` | 获取考勤 |
| `POST` | `/api/lessons/:id/attendance` | 批量保存考勤 |
| `GET` | `/api/lessons/:id/submissions` | 获取全班作业状态 |
| `POST` | `/api/lessons/:id/submissions` | 为指定学生创建或更新作业 |
| `POST` | `/api/submissions/:id/images` | 追加作业图片 |
| `POST` | `/api/submissions/:id/analyze` | 分析指定学生作业 |
| `POST` | `/api/lessons/:id/analyze` | 分析本课次全部待分析作业 |
| `POST` | `/api/mistakes/:id/review` | 确认、修改或忽略分析结果 |
| `GET` | `/api/lessons/:id/insights` | 获取班级学情 |
| `GET` | `/api/lessons/:id/students/:studentId/feedback` | 获取学生反馈 |

所有写接口继续遵守现有账号权限规则。学生账号不得修改考勤、课堂记录、作业归属、错题和报告。

---

## 10. AI 输出与安全规则

调用 AI 时，服务端必须直接传入老师选定的 `studentId`，AI 返回结构中不需要 `studentName`。

建议输出格式：

```json
{
  "mistakes": [
    {
      "question": "第 3 题",
      "knowledgePoint": "数字方阵规律",
      "errorType": "审题错误",
      "status": "confirmed",
      "confidence": 0.91,
      "reason": "学生遗漏了题目中的边界条件"
    }
  ]
}
```

服务端必须进行二次校验：

- AI 返回结果不得修改 `studentId`；
- `confidence` 低于阈值时强制设为 `pending`；
- 非 `confirmed` 结果强制设为 `pending`；
- 缺少题目依据时不得生成确定性错题；
- 没有作业图片时不得生成作业分析；
- 缺勤或未交作业时不得编造学习表现；
- 报告内容必须区分事实、AI 判断和老师备注。

---

## 11. 班级学情规范

班级学情至少包含：

- 考勤汇总；
- 作业收交汇总；
- 作业分析完成度；
- 各知识点错误人数；
- 高频错题；
- 错误类型分布；
- 待确认数量；
- 需要重点关注的学生；
- 下一节课教学建议。

每项统计应能够下钻查看对应学生、错题和作业图片。

不得将下列学生直接计入“未掌握”：

- 缺勤学生；
- 无需提交作业的学生；
- 尚未提交作业的学生；
- 作业尚未分析完成的学生。

这些情况应单独展示。

---

## 12. 学生反馈规范

学生反馈建议包含：

- 本节课考勤；
- 课堂表现；
- 作业提交情况；
- 做得好的地方；
- 当前主要问题；
- 对应题目和图片证据；
- 改进建议；
- 老师补充。

特殊情况必须使用客观表述：

- 缺勤：本节课学生缺勤或请假，暂无完整课堂表现和作业证据；
- 未交作业：学生本节课正常到课，但尚未提交作业，暂不生成作业掌握情况；
- 分析中：作业已上传，分析尚未完成；
- 无课堂记录：本节课暂无额外课堂表现记录，不代表表现正常或异常。

反馈生成后允许老师编辑、复制和重新生成，但不得覆盖原始证据。

---

## 13. 多智能体功能处理

保留现有多智能体底层能力，但普通老师界面优先显示具体任务：

- 生成班级学情；
- 生成学生反馈；
- 给出下一节课教学建议；
- 检查报告是否存在无证据推断。

“智能体名称、灵魂文件、模型绑定”等功能放在高级设置，不作为核心工作流的必经步骤。

---

## 14. 本版本不实现

以下内容不属于 V0.2 范围：

- AI 识别学生姓名；
- AI 自动匹配班级名单；
- AI 生成或补充课程名称；
- 学生自行上传作业；
- 家长端；
- 云端同步；
- 新增更多模型厂商；
- 复杂排课系统；
- 根据缺少的数据自动推断学生表现。

---

## 15. 数据迁移与兼容要求

1. 不得删除现有 `data.json` 中的用户数据。
2. 修改数据结构前必须做好旧字段兼容和迁移。
3. 现有 `assignments`、`performances`、`mistakes` 应尽量迁移到课次语义。
4. 迁移过程必须可重复执行，重复启动不得产生重复数据。
5. 新增数据结构后，应为 `data.json` 增加数据版本字段。
6. 修改前应自动生成本地备份。
7. 原有账号、班级、学生、座位、课堂记录和图片路径必须继续有效。

---

## 16. 开发约束

1. 优先完成数据模型和后端接口，再调整前端工作台。
2. 不要一次性删除旧功能；先完成新流程，再迁移旧入口。
3. 每完成一个阶段都应使用真实本地数据验证。
4. 所有删除操作必须二次确认。
5. 上传、分析和报告生成失败时必须显示明确错误，不得静默失败。
6. AI 分析应允许失败重试，不得因单个学生失败影响整个课次。
7. 代码修改不得覆盖项目中已有但尚未提交的用户改动。
8. 保持本地优先，业务数据和作业图片仍保存在本机。

---

## 17. 推荐开发顺序

### 第一阶段：数据基础

- 明确课次模型；
- 增加考勤结构；
- 增加学生作业提交结构；
- 增加数据版本和迁移；
- 补齐相应接口。

### 第二阶段：课次工作台

- 首页课次卡片；
- 创建课次；
- 课次状态流转；
- 考勤名单；
- 将座位和课堂表现接入课次。

### 第三阶段：课后作业

- 按学生上传；
- 连续录入；
- 作业收交状态；
- 单人及整课次分析；
- 补交和追加图片。

### 第四阶段：待确认

- 集中审核；
- 确认、修改、忽略；
- 重复题目处理；
- 状态和数量联动。

### 第五阶段：学情与反馈

- 班级学情；
- 学生反馈；
- 证据下钻；
- 编辑、复制和重新生成。

---

## 18. 验收标准

编码 Agent 完成后，至少验证以下场景：

1. 老师创建课次后，可以看到完整班级名单和默认考勤。
2. 老师可以将学生改为迟到、请假、缺勤或早退。
3. 老师可以在座位图中记录当前课次的课堂表现。
4. 下课后可以将课次切换为“等待作业”。
5. 老师选择指定学生后可以上传一张或多张作业图片。
6. 系统不会尝试通过 AI 识别学生姓名或改变作业归属。
7. 同一学生可以追加或补交作业图片。
8. AI 可以分析指定学生的作业，低置信度结果自动进入待确认。
9. 单个学生分析失败不会影响其他学生。
10. 老师可以确认、修改或忽略待确认结果。
11. 首页可以看到每个课次的考勤、作业和待确认进度。
12. 班级学情不会把缺勤、未交或尚未分析的学生错误计入“未掌握”。
13. 学生反馈可以区分考勤、课堂表现和作业证据。
14. 没有证据时，系统不会生成确定性的学生评价。
15. 旧班级、学生、座位、课堂表现和错题数据在升级后仍然可用。

最终体验标准：

> 老师能够围绕一节课完成“考勤—课堂记录—课后收作业—AI 分析—确认—查看学情与反馈”的完整闭环，不需要理解多个分散页面之间的关系。
